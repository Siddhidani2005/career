import { redirect } from "next/navigation";
import { SessionType } from "./types";

/////////////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////////////

function LoginChecker({ session }: { session: SessionType | null }) {
  ///////////////////////////////////////////////////////////////////////////////////////////////////////////////////

  if (!session?.user) redirect("/auth/login");

  ///////////////////////////////////////////////////////////////////////////////////////////////////////////////////

  return null;
}

export default LoginChecker;
