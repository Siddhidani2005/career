// Minimal font stub used by components that reference `calsans.className`.
// If you have a custom font loader or Tailwind utility, replace this with the real implementation.

export const calsans = {
  className: "font-sans",
};
